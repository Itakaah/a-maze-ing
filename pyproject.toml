[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "Standalone maze generator and solver module"
requires-python = ">=3.10"
readme = "README.md"

[tool.setuptools.packages.find]
include = ["mazegen*"]
